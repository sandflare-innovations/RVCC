# Server
PORT=4000
NODE_ENV=development

# Postgres (required)
DATABASE_URL=postgresql://user:pass@host:5432/rvcc

# CORS — comma-separated frontend origins (no trailing slash). Never use *.
ALLOWED_ORIGINS=https://rvcc-prod.pages.dev,https://rvcc-vendor.pages.dev,https://rvcc-admin.pages.dev,https://rvcc-prod.vercel.app,http://localhost:3000,http://localhost:3001,http://localhost:3002

# Public site URL used in approval / requirement emails (vendor portal host)
VENDOR_PORTAL_URL=https://rvcc-vendor.pages.dev
# Optional HMAC secret for email-gate tokens after OTP (defaults to DATABASE_URL)
# EMAIL_GATE_SECRET=generate-a-long-random-secret

# SMTP (enquire OTP, submission, decision, requirement mail)
SMTP_HOST=smtp.example.com
SMTP_PORT=587
SMTP_SECURE=false
SMTP_USER=
SMTP_PASS=
ENQUIRE_FROM_EMAIL=RVCC Procurement <procurement@example.com>
