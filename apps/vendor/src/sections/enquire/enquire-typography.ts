/** Shared typography for supplier registration in the portal. */

export const enquireEyebrowClass = "text-sm font-bold tracking-[0.2em] text-brand-blue uppercase";

/** Page titles — brand display font with readable letter-spacing (not tracking-tighter). */
export const enquirePageTitleClass =
  "font-heading text-4xl leading-[0.95] tracking-[0.04em] uppercase md:text-6xl";

export const enquirePageSubtitleClass = "max-w-2xl text-lg leading-relaxed text-zinc-600";

/** In-step section headings (Contact 1, Address 2, etc.). */
export const enquireSectionTitleClass =
  "text-base font-bold tracking-[0.1em] text-zinc-700 uppercase";

/** Intro / helper copy under the page header. */
export const enquireMutedClass = "text-base leading-relaxed text-zinc-600";

/** Secondary hints and empty states. */
export const enquireHelperClass = "text-base leading-relaxed text-zinc-500";

/** Remove / add row actions. */
export const enquireActionLinkClass =
  "inline-flex min-h-[44px] items-center text-sm font-bold tracking-wide text-zinc-600 uppercase transition-colors hover:text-red-600";

/** Step train label under each circle. */
export const enquireStepLabelClass =
  "hidden text-xs font-semibold tracking-[0.1em] whitespace-nowrap uppercase sm:block sm:text-sm";

/** Checkbox / option row labels. */
export const enquireOptionLabelClass = "text-base text-zinc-700";
